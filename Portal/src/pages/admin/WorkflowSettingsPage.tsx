import PageShell from '../../components/common/PageShell';

export default function WorkflowSettingsPage() {
  return <PageShell title="Workflow Settings"><p>Configure email templates and workflow parameters.</p></PageShell>;
}
