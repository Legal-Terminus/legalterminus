import PageShell from '../../components/common/PageShell';

export default function TasksPage() {
  return <PageShell title="Admin — All Tasks"><p>Task management table for admins.</p></PageShell>;
}
