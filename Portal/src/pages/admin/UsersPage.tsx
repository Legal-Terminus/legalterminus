import PageShell from '../../components/common/PageShell';

export default function UsersPage() {
  return <PageShell title="User Management"><p>Create, view, and manage user accounts.</p></PageShell>;
}
